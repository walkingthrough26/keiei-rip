A large Mincho pull-quote / epitaph with a vermilion left rule. Use for a founder's last words or a closing line in a case.

```jsx
<Quote cite="創業者 田中" source="閉店の日に">
  最後の一日まで、店は満席だった。
</Quote>
```

Pass `cite` (person) and `source` (context). `onDark` for sumi bands.
