Squared toggle chip for filtering the archive by 業種 / 年代 / 死因. Active = sumi fill; optional mono `count`.

```jsx
<FilterChip active>すべて<></FilterChip>
<FilterChip count={42} onClick={...}>飲食</FilterChip>
<FilterChip count={11}>資金繰り</FilterChip>
```
